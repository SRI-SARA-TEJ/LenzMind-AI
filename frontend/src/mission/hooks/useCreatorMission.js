/**
 * mission/hooks/useCreatorMission.js
 *
 * Public API hook for consuming CreatorMissionContext.
 * Components import this — not the context directly.
 */

export { useCreatorMissionContext as useCreatorMission } from '../context/CreatorMissionContext';
